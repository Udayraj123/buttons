---
layout: component.hbs
---
{{#each button}}
<section id="{{@key}}-{{../demo.component}}">
  <h2>{{titleize @key}} {{../button.expand-right.label}}</h2>
  {{> button .}}

</section>
{{/each}}
